config:
  target: "http://localhost:3003"  # The endpoint for the orders service
  phases:
    - duration: 60  # Test for 60 seconds
      arrivalRate: 100  # 5 virtual users per second
scenarios:
  - flow:
      - post:
          url: "/orders"
          json:
            userId: 1
            productId: 1
      - get:
          url: "/orders"
