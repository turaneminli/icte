config:
  target: "http://127.0.0.1:3003" # The endpoint for the orders service
  phases:
    - duration: 120 # Test for 60 seconds
      arrivalRate: 10 # 5 virtual users per second
scenarios:
  - flow:
      - post:
          url: "/orders"
          json:
            userId: 1
            productId: 1
      - get:
          url: "/orders"
